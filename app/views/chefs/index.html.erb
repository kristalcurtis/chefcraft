<h1>Listing chefs</h1>

<table>
  <tr>
    <th>Username</th>
  </tr>

<% @chefs.each do |chef| %>
  <tr>
    <td><%=h chef.username %></td>
    <td><%= link_to 'Show', chef %></td>
    <td><%= link_to 'Edit', edit_chef_path(chef) %></td>
    <td><%= link_to 'Destroy', chef, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New chef', new_chef_path %>