<h1>Listing recipe_boxes</h1>

<table>
  <tr>
  </tr>

<% @recipe_boxes.each do |recipe_box| %>
  <tr>
    <td><%= link_to 'Show', recipe_box %></td>
    <td><%= link_to 'Edit', edit_recipe_box_path(recipe_box) %></td>
    <td><%= link_to 'Destroy', recipe_box, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New recipe_box', new_recipe_box_path %>