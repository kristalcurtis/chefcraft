<h1>Listing history_entries</h1>

<table>
  <tr>
  </tr>

<% @history_entries.each do |history_entry| %>
  <tr>
    <td><%= link_to 'Show', history_entry %></td>
    <td><%= link_to 'Edit', edit_history_entry_path(history_entry) %></td>
    <td><%= link_to 'Destroy', history_entry, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New history_entry', new_history_entry_path %>